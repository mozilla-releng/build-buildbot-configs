Buildbot configs for signing our maemo deb repos.

	ln -s config-staging.py config.py
	ln -s master-staging.cfg master.cfg

Make sure that the master-main.cfg is in the same directory if you're
copying/linking from the source.
